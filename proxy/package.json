{
  "name": "inktonermoore-ai-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies AI Mode routing requests to Gemini Flash-Lite",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
